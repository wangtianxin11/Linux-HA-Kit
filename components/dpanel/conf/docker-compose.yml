services:
  dpanel:
    image: dpanel/dpanel:lite
    pull_policy: never
    container_name: dpanel
    restart: always
    ports:
      - "__DPANEL_PORT__:8080"
    environment:
      - APP_NAME=dpanel
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/dpanel:/dpanel
